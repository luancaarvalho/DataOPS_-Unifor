# ========================================
# DataOps Pipeline - Configuração de Ambiente
# ========================================
#
# INSTRUÇÕES:
# 1. Copiar este arquivo para .env: cp .env.example .env
# 2. Os valores abaixo funcionam para desenvolvimento/teste
# 3. IMPORTANTE: Altere as credenciais para produção!
# 4. NÃO committar .env no git (está no .gitignore)
#

# ========================================
# MinIO Configuration
# ========================================
# Credenciais para acessar o MinIO (S3-like storage)
# ATENÇÃO: Valores padrão para DEV/TESTE. Altere para PRODUÇÃO!
MINIO_ACCESS_KEY=dataops_admin
MINIO_SECRET_KEY=DataOps2025!SecurePassword

# OPCIONAL: Endpoint do MinIO (auto-detectado)
# - Se VAZIO: detecta automaticamente (localhost:9000 local, minio:9000 no Docker)
# - Se PREENCHIDO: usa o valor especificado
# MINIO_ENDPOINT=localhost:9000

# ========================================
# Label Studio Configuration
# ========================================
# Token de autenticação do Label Studio
# IMPORTANTE: Você DEVE obter o token após criar conta no Label Studio
# Como obter:
#   1. Acesse http://localhost:8001
#   2. Crie conta com: label_ops@gmail.com / dataops@123
#   3. Organization > API Tokens Settings > Marque APENAS "Legacy tokens"
#   4. Account & Settings > Copie "Legacy API Token"
LABELSTUDIO_TOKEN=SUBSTITUIR_PELO_SEU_TOKEN_AQUI
LABELSTUDIO_PROJECT=4

# OPCIONAL: URL do Label Studio (auto-detectado)
# - Se VAZIO: detecta automaticamente (localhost:8001 local, label-studio:8080 no Docker)
# - Se PREENCHIDO: usa o valor especificado
# LABELSTUDIO_URL=http://localhost:8001

# ========================================
# Airflow Configuration
# ========================================
# UID do usuário airflow (para permissões de volume)
# Deixar padrão se não souber
AIRFLOW_UID=50000

# Usuário e senha para webui do Airflow
# (podem ser customizados no docker-compose.yml)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# ========================================
# Variáveis Opcionais
# ========================================
# Adicionar dependências Python extras
_PIP_ADDITIONAL_REQUIREMENTS=

# ========================================
# IMPORTANTE - CHECKLIST ANTES DE USAR
# ========================================
# ☑ [ ] Copiou .env.example para .env?
# ☑ [ ] Obteve LABELSTUDIO_TOKEN no Label Studio (ver instruções acima)?
# ☑ [ ] Verificou que LABELSTUDIO_PROJECT está correto (padrão: 4)?
# ☑ [ ] Se for PRODUÇÃO: alterou MINIO_ACCESS_KEY e MINIO_SECRET_KEY?
# ☑ [ ] Confirmou que .gitignore contém .env?
# ☑ [ ] NÃO commitou arquivo .env com credenciais reais?

# ========================================
# CREDENCIAIS PADRÃO PARA ACESSO
# ========================================
# MinIO Console (http://localhost:9001):
#   User: dataops_admin
#   Pass: DataOps2025!SecurePassword
#
# Airflow (http://localhost:8080):
#   User: airflow
#   Pass: airflow
#
# Label Studio (http://localhost:8001):
#   Email: label_ops@gmail.com
#   Pass: dataops@123
#   (Criar conta no primeiro acesso)
#
# Streamlit Dashboard (http://localhost:8501):
#   Sem autenticação
